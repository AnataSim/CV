rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Global rule allowing read & write operations through September 28, 2026
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 9, 28);
    }
  }
}
